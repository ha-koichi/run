# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'run' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for run
  pod 'Firebase/Analytics'

  target 'runTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'runUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
